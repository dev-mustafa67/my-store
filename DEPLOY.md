# خطوات تشغيل التطبيق كموقع حقيقي (بدون تيرمنال — عبر المتصفح فقط)

## الخطوة 1: قاعدة البيانات (Supabase) — 5 دقائق
1. افتح supabase.com وسجّل حساباً مجانياً → "New Project".
2. اذهب إلى "SQL Editor" داخل المشروع → افتح ملف `sql/schema.sql` من هذا المجلد → انسخ محتواه بالكامل والصقه هناك → اضغط Run.
3. من قائمة الإعدادات (Settings → API)، انسخ قيمتين: `Project URL` و `anon public key` — ستحتاجهما في الخطوة 3.
4. من "Table Editor" أضف صفاً واحداً يدوياً في جدول `stores` باسم محلك (مثلاً "محل الأناقة").
5. من "Authentication → Users" أنشئ أول مستخدم (بريدك وكلمة مرور) — هذا سيكون حساب المالك.
6. ارجع لـ Table Editor → جدول `users_profile` → أضف صفاً يربط الـ id الخاص بالمستخدم الذي أنشأته بـ id المحل، مع role = owner.

## الخطوة 2: رفع الكود إلى GitHub — 5 دقائق
1. افتح github.com وسجّل حساباً مجانياً → اضغط "New repository" → أعطه اسماً (مثلاً inventory-app) → Create.
2. داخل الصفحة الفارغة للمستودع، اضغط "uploading an existing file" → اسحب كل ملفات ومجلدات هذا المشروع إليها دفعة واحدة → اضغط Commit.

## الخطوة 3: النشر على الإنترنت (Vercel) — 3 دقائق
1. افتح vercel.com وسجّل الدخول باستخدام حساب GitHub نفسه.
2. اضغط "Add New Project" → اختر المستودع الذي رفعته للتو.
3. قبل الضغط على Deploy، افتح قسم "Environment Variables" وأضف القيمتين اللتين نسختهما من Supabase:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. اضغط Deploy وانتظر دقيقة إلى دقيقتين — ستحصل على رابط حقيقي (مثل inventory-app.vercel.app) يفتحه أي شخص من هاتفه أو حاسوبه.

## الخطوة 4: أول تجربة حقيقية
افتح الرابط → سجّل دخول بالبريد وكلمة المرور اللذين أنشأتهما في الخطوة 1 → أضف منتجاً من تبويب "المنتجات" → أضف زبوناً من تبويب "الزبائن" → بِعه منتجاً من "الكاشير" (جرّب تفعيل "بيع بالدين") → راقب النتيجة في "الديون" و"التحليلات" (يشمل توصيات إعادة الطلب).

كل تعديل تعمله لاحقاً على الكود (رفع نسخة جديدة على GitHub بنفس طريقة السحب والإفلات) يُحدَّث الموقع تلقائياً خلال دقيقة دون أي خطوة إضافية منك.
